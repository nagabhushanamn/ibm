/**
 * http://usejsdoc.org/
 */

require(null, [ 'js/a' ], function(amod) {

	amod.doCount();
	amod.doCount();
	amod.doCount();
	amod.doCount();
	amod.doCount();
	
	
	console.log(amod.getCount());

});